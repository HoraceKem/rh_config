import setuptools

setuptools.setup(
    description="Configuration for Rhoana pipeline",
    install_requires=[
        "pyaml>=15.8.2"
        ],
    name="rh_config",
    packages=["rh_config"],
    url="https://github.com/Rhoana/rh_config",
    version="0.1.0"
)
